# 魔法书剧本格式规范 v0（MBS · Magic Book Script）

正式版游戏与未来创意工坊共用的剧本文件格式。本文档是引擎实现与剧本创作的
唯一依据；格式变更必须先改本文档并递增格式版本号。

- 当前格式版本：`0.1`
- 引擎实现：`js/engine.js`
- 样例剧本：`scripts/case1.zh.js`（第一案竖切片）

## 0. 设计原则

1. **散文优先**。剧本以纯文本行为主，标记越少越好——作者大部分时间在写故事，
   不是在写代码。
2. **声明式**。剧本只描述「是什么」，不写过程代码。表现力靠引擎内置的具名
   词表（特效、音效、玩法模块）扩充，不开放任意脚本——这是工坊安全的地基。
3. **ID 稳定**。一切可交互元素显式命名。显示文字可以随文案与翻译任意改，
   ID 永不变——存档、翻译、工坊分享全部依赖它。
4. **可校验**。格式的每条规则都要能被 lint 机械检查（见 §8）。

## 1. 四层模型

| 层 | 内容 | 本版状态 |
|---|---|---|
| 表现层 | 文字演出、说话人、背景层栈、三轨音频、具名特效 | 部分实现 |
| 交互层 | 互动词、道具/弹种、线索、玩法模块槽 | 核心实现 |
| 逻辑层 | flag、条件、跳转、自动触发 | 实现 |
| 工程层 | 元数据、稳定 ID、格式版本、lint、i18n | 规范先行 |

## 2. 文件组织与 i18n

- 一章一文件：`scripts/<章节id>.<语言>.js`，如 `case1.zh.js`、`case1.ja.js`。
- 文件内容是一个注册到 `MB_SCRIPTS` 的纯文本字符串（用 `String.raw` 包裹，
  正文中不得出现反引号）。用 JS 文件而非 fetch 是为了 `file://` 本地双击可玩。
- **翻译单位是整个剧本文件**：各语言文件结构同构，仅翻译显示文字；
  场景 id、词 id、flag、道具 id 在所有语言中必须一致。
- 因此互动词**必须显式写 id**：`[词语|word_id]`。省略 id（以词语本身为 id）
  仅允许在草稿阶段使用，lint 会警告。

## 3. 语法

逐行解析。`//` 开头为注释行。缩进两格表示块内动作。

```
@meta                      // 章节元数据
  id: case1
  title: 第一案 · 雨与当铺
  protagonist: 李维        // 常驻 HUD：姓名
  badge: 2077-09           // 常驻 HUD：警号（本章容器代数）
  version: 0.1             // 剧本内容版本
  format: 0.1              // 所依赖的格式版本
  start: office            // 起始场景

@items                     // 道具与弹种（子弹=动词，解锁后不消耗）
  bullet_read: 🔵 读取弹 | 命中后展开目标浅层记忆。

@act 第一幕 · 科室          // ===== 幕：其后的场景都属于这一幕 =====
                           // 页码按幕内编号；不写 @act 则整章一幕

@scene office              // ===== 场景 =====
  label: 特殊记忆侦查科     // 可选：场景分隔标题
  bg: #15171f              // 背景层：纯色（图片层/HTML 层为预留，见 §7）
  amb: rain                // 环境音轨（rain / office / off）
  fx: rain                 // 背景效果层（rain / off）
  mode: memory             // 可选：记忆态渲染（文字质感区别于现实）
  style: coda              // 可选：章尾回顾旁白样式

  这是第 1 页的旁白。
  科长: 这是一句台词。
  李维(内心): 这是内心独白。
  ! 这是一行强调（举证、爆点）。
  正文中嵌入互动词：请看这扇[气窗|window]。

  @page                    // 手动分页：页是叙事节拍，由作者控制
  这是第 2 页。

  @page need saw_window | 先把现场查清楚。
                           // 门槛页：条件不满足时点「下一页」弹提示不放行
  这是第 3 页，进得来说明条件已满足。

  + window (once):         // 点击反应块；(once) 执行后置灰
      : 块内的旁白行。
      clue 窗框上有新撬痕
      set saw_window
  + suspect @ bullet_read: // 对词使用道具/子弹的反应块
      goto memory_dive
  when saw_window && got_tool: goto report   // 场景级自动触发（一次性）
```

### 3.1 文本行

| 写法 | 含义 |
|---|---|
| `任意文字` | 旁白 |
| `名字: 文字` | 台词（名字 ≤10 字，中英数字） |
| `名字(内心): 文字` | 内心独白（弱化样式） |
| `! 文字` | 强调行（举证/爆点样式） |
| `[词语|id]` | 行内互动词，可出现在任何文本行中 |

### 3.2 块内动作词表

| 动作 | 含义 |
|---|---|
| `: 文字` / `名字: 文字` / `! 文字` | 输出文本（同 §3.1） |
| `get id` / `take id` | 获得 / 移除道具 |
| `set flag` / `unset flag` | 置 / 清 flag |
| `clue 文字` | 记录一条线索（卷宗库可查） |
| `hint 文字` | 底部提示条闪现 |
| `sfx 名字` / `fx 名字 [参数]` / `amb 名字` | 音效 / 特效 / 环境音 |
| `goto scene_id` | 跳转场景（其后动作不执行） |
| `off` / `off word_id` | 置灰本词 / 指定词 |
| `archive 标题 | 正文` | 写入卷宗库（归档案卷） |
| `if 条件: 任意单行动作` | 条件执行 |

条件语法：`flag`、`!flag`、`a && b`；道具 id 也可作为条件项（持有即真）。

## 4. 具名特效 / 音效 / 环境音词表

引擎内置实现，剧本只引用名字。扩充词表 = 扩充引擎，剧本格式不变。

- **fx**：`rain` 文字雨 ｜ `flash` 雷闪 ｜ `shake` 震屏 ｜ `dissolve` 文字溃散
  （记忆强制中断）｜ `stamp 文字` 归档盖章 ｜ `off` 关闭背景效果层
- **sfx**：`paper` `knock` `get` `clue` `stab` `unlock` `dive`（记忆潜入）
  `interrupt`（强制中断）`stamp` `slam` `buzz`
- **amb**：`rain` 雨夜 ｜ `office` 空调与键盘 ｜ `off`
- 三轨模型：amb（循环底噪）/ sfx（一次性）/ voice（语音，预留）。
  音量与开关按轨独立（当前实现为总开关，按轨为 P1）。

## 5. 交互模型

- 点击互动词 → 执行其 `+ id:` 块；选中道具后点击 → 执行 `+ id @ item:` 块；
  无对应块时抖动并提示。
- **子弹即动词**：读取弹、删改弹、替换弹等以道具形式发放，解锁后不消耗
  （硬规则：子弹不做消耗品，避免推理流程死锁）。
- **玩法模块槽**（预留语法）：`@minigame 名字 参数…` 嵌入对质举证、连击战等
  内置玩法，模块由引擎实现并随版本扩充。

## 5.5 展现模型（引擎行为）

- **幕 ＞ 场景 ＞ 页**：页由作者用 `@page` 手动划分，是叙事节拍单位；
  `@page need 条件 | 提示` 构成推进门槛——要素没集齐就不放行，并给出
  逆转裁判式的提示。页码条显示幕标签与幕内页码（每幕重新编号）。
- 文本播放期间任何点击都是「继续」，保证每句话完整呈现；播放完毕后，
  点击互动词在**本页就地**展开内容，点击空白处翻页/解锁下一页。
- 旧页可回翻且保持可互动，含未点击元素的页在页码上亮点提示；
  每页可加书签（🔖），「检索」面板提供书签列表与全文搜索。

## 6. 存档与卷宗

- 存档即「记忆槽」：场景级 checkpoint，进入场景时自动同步
  （localStorage，键 `mb-save-<章节id>`），内容为 `{场景, flags, 道具, 线索, 卷宗}`。
- 读档 = 从该场景开头重新载入。文案与 UI 一律使用剧情内措辞（记忆同步 / 槽位 / 警号）。
- 卷宗库：`archive` 写入的条目跨场景可查；**双版本预留**——每条卷宗可挂
  `tampered` 替换版本，引擎按 flag 决定展示哪份（被篡改卷宗 + 涂黑动画，P1）。

## 7. 能力分级（工坊安全）

| 级别 | 可用能力 |
|---|---|
| 官方剧本 | 全部，含未来的 HTML 背景层 |
| 工坊剧本 | 仅本规范词表内的声明式能力；无任意 HTML/JS；资源仅白名单外链或内置 |

## 8. Lint 规则（规范，v0 未实现）

1. `goto` 目标场景必须存在；2. 互动词的 id 必须有对应块或被显式标记为纯装饰；
3. 块引用的 flag/道具必须在某处被 set/get；4. 起始场景必达每个场景（不可达警告）；
5. 省略 id 的互动词 → i18n 警告；6. 软锁检测：通关所需 flag 的赋值路径不可被
   一次性资源阻断；7. `format` 版本高于引擎支持版本 → 拒绝加载。

## 9. 路线图

- P0（已实现）：本规范核心 + 第一案竖切片
- P1：背景图片层/层栈过渡、立绘/头像、按轨音量、双版本卷宗与涂黑动画、
  打字机/逐字演出、难度档（提示强弱/阅读模式）、lint 工具
- P2：剧本编辑器 UI、工坊分发、语音轨
